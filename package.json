{
  "name": "howsee",
  "version": "0.1.0",
  "private": true,
  "description": "PicPick AI photo prompt generator MVP.",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/dev-server.js",
    "start": "npm run dev",
    "build": "node scripts/build-static.js",
    "check": "node --check src/picpick-core.js && node --check src/app.js && node --check scripts/dev-server.js && node --check scripts/build-static.js && node --check test/picpick.test.js"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
