{
  "name": "howsee",
  "version": "0.1.0",
  "private": true,
  "description": "Adult-only prompt rewriting prototype with safety guardrails.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/translator.js && node --check test/translator.test.js"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
